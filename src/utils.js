/* eslint-disable import/prefer-default-export */
export const getRandomNumber = (min, max) => Math.floor(Math.random() * (max - min)) + min;

/* функция принимает два аргумеиента min and max (минимальное число и максимальное)
ипользуется формула math.floor для получения случайного числа в заданом диапозоне
*/
